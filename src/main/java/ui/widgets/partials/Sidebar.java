package ui.widgets.partials;

import ui.helper.Widget;
import org.openqa.selenium.By;

public class Sidebar extends Widget<Sidebar> {

    public Sidebar(By locator) {
        super(locator);
    }
}
